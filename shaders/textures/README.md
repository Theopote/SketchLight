# Paper Texture Asset

Place the paper texture at this path:

`paper.png`

Use a seamless, neutral gray-white paper or canvas texture in 8-bit RGB/RGBA PNG format.
Recommended dimensions are 512 x 512 or 1024 x 1024 pixels. Do not include a border or
large directional marks, because `final.fsh` tiles this image across the screen.